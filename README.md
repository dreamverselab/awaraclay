
  # Awara Clay

  This is a code bundle for Static HTML Page Creation. The original project is available at https://www.figma.com/design/EpaBAfSrPDlsv7jaLm0hff/Static-HTML-Page-Creation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
